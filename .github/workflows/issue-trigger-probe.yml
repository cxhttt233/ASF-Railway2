name: issue trigger probe

on:
  issues:
    types: [opened]

jobs:
  probe:
    if: contains(github.event.issue.title, '[probe]')
    runs-on: ubuntu-latest
    steps:
      - run: echo "issue-triggered action works"
